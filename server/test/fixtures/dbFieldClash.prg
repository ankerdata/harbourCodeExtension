PROCEDURE DbFieldClash()
   // A field sharing its alias's name, referenced before the others.
   ? ord->ord
   ? ord->total
   ? ord->status
   // Same field again in different casing.
   ? ord->Total
RETURN
